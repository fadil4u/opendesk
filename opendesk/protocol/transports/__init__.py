"""Concrete :class:`~opendesk.protocol.connection.Connection` transports.

* :mod:`~opendesk.protocol.transports.websocket` — WebSocket binary frames
  over TCP (with optional TLS).  This is the v1 default transport.
* (later) QUIC, unix socket, …
"""
